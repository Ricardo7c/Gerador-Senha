[package]
name = "gerador_senha"
version = "0.1.0"
edition = "2024"

[dependencies]
# Desativa os padrões (que puxam wgpu/vulkan) e usa glow (OpenGL)
eframe = { version = "0.36.1", default-features = false, features = ["default_fonts", "glow", "wayland", "x11"] }
sha2 = "0.11.0"
